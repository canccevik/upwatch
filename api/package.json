{
  "name": "upwatch",
  "scripts": {
    "build": "lerna run build --stream",
    "start": "lerna run start --stream",
    "start:dev": "lerna run start:dev --stream",
    "lint": "lerna run lint --stream"
  },
  "dependencies": {},
  "devDependencies": {
    "@upwatch/prettier-config": "workspace:^",
    "lerna": "^8.1.9"
  }
}
